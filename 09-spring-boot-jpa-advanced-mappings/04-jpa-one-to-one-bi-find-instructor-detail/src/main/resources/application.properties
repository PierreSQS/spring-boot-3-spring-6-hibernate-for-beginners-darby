# ENABLE COLOR ON TERMINAL ENTRIES
spring.output.ansi.enabled=ALWAYS

# SHOW SQL
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.show_sql=true

# SQL SYNTAX HIGHLIGHTING
spring.jpa.properties.hibernate.highlight_sql=true

# FORMAT SQL
spring.jpa.properties.hibernate.format_sql=true

# SHOW BIND VALUES
# New with Hibernate 6.x
logging.level.org.hibernate.orm.jdbc.bind=trace

# SHOW JPA/HIBERNATE LOGGING MESSAGES
logging.level.org.hibernate.SQL=trace

# New to SB3.4.0! Structured logging
# possible values logstash, ecs, gelf
# logging.structured.format.console=gelf

# we still use the same DB-Schema, although we have a Bi-Directional relationship,
# we just make the change in the InstructorDetail Entity
spring.datasource.url=jdbc:mysql://localhost:3307/hb-01-one-to-one-uni
spring.datasource.username=springstudent
spring.datasource.password=springstudent

# Turn off the Spring Boot banner
#spring.main.banner-mode=off

# Reduce logging level. Set logging level to warn
#logging.level.root=warn
